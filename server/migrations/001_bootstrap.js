/**
 * Bootstrap migration.
 * This migration is intentionally minimal and records the baseline
 * so future changes can be tracked with migrations.
 */

export async function up() {
  // No-op by design.
}
